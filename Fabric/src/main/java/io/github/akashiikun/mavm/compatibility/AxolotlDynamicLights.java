package io.github.akashiikun.mavm.compatibility;
/*
import dev.lambdaurora.lambdynlights.api.DynamicLightsInitializer;
import net.minecraft.world.entity.EntityType;
import net.minecraft.world.entity.animal.axolotl.Axolotl;

import static dev.lambdaurora.lambdynlights.api.DynamicLightHandlers.registerDynamicLightHandler;

public class AxolotlDynamicLights implements DynamicLightsInitializer {

    @Override
    public void onInitializeDynamicLights() {
        registerDynamicLightHandler(EntityType.AXOLOTL, entity -> {
            int luminance = 0;
            Axolotl.Variant variant = entity.getVariant();
            if (variant.getName().equals("glowxolotl"))
                luminance = 15;
            return luminance;
        });
    }

}*/
